export default function RolesPage() {
  return (
    <main className="shell px-4 py-6 sm:px-6">
      <section className="glass-card mx-auto w-full max-w-5xl rounded-[1.5rem] px-6 py-6">
        <p className="text-lg font-semibold text-[#2d3150]">Roles</p>
        <p className="mt-2 text-sm text-[var(--muted)]">Role management screens will land here next. Permissions CRUD is ready first.</p>
      </section>
    </main>
  );
}
